services:
  - type: web
    name: inkle-tourism-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn src.main:app --host 0.0.0.0 --port=10000"
    envVars:
      - key: USER_AGENT
        value: "inkle-tourism-assignment/1.0 (contact: rpr22ainds@cmrit.ac.in)"
